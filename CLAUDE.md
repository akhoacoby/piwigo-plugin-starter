# CLAUDE.md — Piwigo plugin starter

**Canonical instructions: [`AGENTS.md`](AGENTS.md).** Read it first — it is the single, tool-neutral source of truth (layout, guidelines, workflows, reference, golden rules).

Claude Code also auto-invokes the **skills** under `.claude/skills/`; each `SKILL.md` is a thin wrapper that points at the canonical `workflows/`/`reference/` doc. Nothing is duplicated — follow the doc it names.
